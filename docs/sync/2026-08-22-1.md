# Sync 2026-08-22-1 — upstream main a4f16e3

- 日期: 2026-08-22
- 上游: `NousResearch/hermes-agent` main `a4f16e3fefdc`(08-22)
- 上一基准: `17d1095443bb`(2026-08-21-1 同步)
- 方式: **git subtree merge**(`scripts/sync-upstream.sh main`)

## 变更

- desktop: 105 文件, +5520/−2374
  - 错误卡增强: Retry / Switch Provider / Send diagnostics / Open Desktop logs
  - 新增诊断上传弹窗与 `diagnostics.share_nous` 调用链
  - tab strip / pane tree 多处行为修正
  - gateway boot / reconnect busy state 修正
  - hermes-bots 插件测试与注册逻辑调整
- shared: 上游树未变；脚本刷新了 filtered split/anchor，无实际内容差异
- subtree 合并零文本冲突；本次依赖无新增/删除，未更新 lockfile

## Web 桥面处理

- 新上游桥面 `logsRoot?: () => Promise<string>`：Web 无本地 Electron profile 日志目录，也无浏览器/gateway 等价 → 不实现；调用方用可选链保护，按钮在 Web 不显示
- 新诊断上传入口：走现有 gateway RPC `diagnostics.share_nous`；Web 已有 `getRecentLogs` 返回最近 renderer error 摘要作为 best-effort extra，无需新增桥面
- 保留 Web 专有接口：`oauthPasteConnectionConfig`、`passwordLoginConnectionConfig`、`saveImageFile`、`releaseBlobFile`、`streamMediaUrl`

## 补丁保留抽查

- `styles.css` 顶部 `@source '../../hermes-desktop/src'` / `@source '../../hermes-shared/src'` ✓
- `chat/index.tsx` voice `enabled: true` ✓
- `global.d.ts` Web 专有 bridge 扩展保留 ✓
- `use-composer-actions.ts` `saveImageFile` / `attachFileBlob` / `releaseBlobFile` ✓
- `lib/media.ts` `streamMediaUrl?.(path)` 优先 ✓
- `apps/web/vite.config.ts` driver.js IIFE alias 与 optimizeDeps.exclude 保留 ✓

## 验证计划

- 本机 `AGENTS.local.md` 禁止跑 typecheck/test/build；只运行轻量/必要命令：
  - `bash scripts/sync-upstream.sh main` ✓
  - `pnpm exec prettier --write PATCHES.md docs/sync/2026-08-22-1.md`（提交前格式化）
- 真实验证走 GitHub Actions `CI`。
