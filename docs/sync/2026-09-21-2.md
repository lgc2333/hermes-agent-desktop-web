# Sync 2026-09-21-2 — upstream main 7b660e66

- 日期: 2026-09-21
- 上游: `NousResearch/hermes-agent` main `7b660e66ee3d` (2026-09-21)
- 上一基准: `080907e3b7ad` (2026-09-21 同步)
- 方式: **git subtree merge** (`scripts/sync-upstream.sh main`)
  - desktop filtered commit: `8ab461cbea99`（anchor 已挂 ref 保护）
  - shared filtered commit: `3a10ca561bac`（anchor 已挂 ref 保护）
- 变更规模: 上游区间 12 commit（其中 5 个动 `apps/desktop`）。vendor desktop 12 文件变更（+823/-21），新增 4 文件；**vendor shared 上游树未变**（`apps/shared` 树 SHA 前后一致），merge 为空合并。
- 合并结果: `src/app/chat/index.tsx` 自动合并（ort），无冲突。

## 变更

- desktop（全部为会话转录窗口/分页内部改动，无新 UI 面）:
  - 新增 `src/app/chat/transcript-retention.ts` + `use-transcript-retention.ts`：store 释放已翻页离开窗口的历史（此前 store 永久持有全部消息，长会话内存单调增长，#77311）；
  - `src/store/transcript-tail.ts`：老页 offset 按**后端行**计数（折叠消息用新增 `serverRowSpan` 上报覆盖行数），释放后再翻页时回卷 offset 走既有 backfill 重新拉取；
  - `src/lib/chat-messages/hydration.ts` / `types.ts` / `tool-parts.ts`：fold 记录 `serverRowSpan`；`use-session-actions/utils.ts` 把 `serverRowSpan` 列入 re-render 忽略字段；
  - 配套单测 4 个（`transcript-retention.test.ts` / `use-transcript-retention.test.tsx` 等）。

## Web 桥面处理（新接口）

本次 `global.d.ts` 与 `hermesDesktop` 表面**均未变**，无新桥面需实现或对齐；改动全部在渲染层/store 内部（释放 + 回卷均走既有 older-page backfill 链路）。Web 侧无需桥层适配。

## 补丁保留抽查（§4）

`git diff refs/subtree-anchors/hermes-desktop HEAD:vendor/hermes-desktop` 结果恰好是 §4 登记的 12 个补丁文件（+498/-74），无多无少；shared 无补丁、diff 为空。

- `src/app/chat/index.tsx`：唯一与上游重叠的文件，自动合并通过，voice `enabled: true` 与注释保留。
- 其余 11 个补丁文件（`styles.css` @source、`global.d.ts` Web 表面、`gateway-settings.tsx` / `first-run-remote-form.tsx` 密码与 paste-back、`boot-failure-overlay.tsx` 路由、`use-keybinds.ts` `VITE_WEB_BUILD`、`use-composer-actions.ts` ADR-0020、`media.ts` ADR-0022、i18n 三件）：上游本次未触及，三方合并原样保留。
- 依赖：上游本次未改任何 `package.json` → 无需 `pnpm install`/锁文件更新。

## 上一轮收尾（CI 红）

上一轮同步（080907e3）推送后 CI 唯一失败点为 `format (prettier --check)`：
`apps/web/src/bridge/registry.ts` 由 `db391b8ca` 引入未格式化。已本地 `prettier -c .` 复现（仅此一文件），
修复后 `--amend` 并入 `db391b8ca`（`--force-with-lease` 推送），不追加独立提交。
