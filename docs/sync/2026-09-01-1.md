# Sync 2026-09-01-1 — upstream main 18c90480

- 日期: 2026-09-01
- 上游: `NousResearch/hermes-agent` main `18c904807033` (2026-09-01)
- 上一基准: `446563262e54` (2026-08-31 同步)
- 方式: **git subtree merge** (`scripts/sync-upstream.sh main`)
  - desktop filtered commit: `c08cac537a69436a3395db8eee8b3fcdba172154`（anchor 已挂 ref 保护）
  - shared filtered commit: `2f110720f385951ed6d35a03413caed122c52484`（anchor 已挂 ref 保护；
    树与上一基准相同，无实际变更）

## 变更

- desktop: 47 文件，+2126/−147（含测试）
  - 新增 `store/runtime-gone.ts`：session runtime 消失（PULL 通道 4001
    "session not found"）的后台轮询自愈（tiles unbind + 主线程 resume 重臂）
  - gateway store：`isAttachedSharedRemote` 探测——attached 的 shared-remote
    connection 的 agent RPC 路由到主 socket（`profile` 参数复用主连接），
    避免 ghost WebSocket；SSH 隔离路由仍各自拨号
  - session owner route 辅助（settings/helpers.ts）+ wiring 接线：旧的
    persist 成 `local` 的 stale hint 清理
  - bot roster actions、command-center delete confirm、config-settings、
    sessions API（session-info 事件扩展）、composer-status、
    connection-registry/ssh-connection（electron 侧）等
- shared: 无实际变更（filtered commit 树相同）

## Web 桥面处理

- **无新增桥面接口**：上游 `global.d.ts` 本次未变。diff 中的
  `hermesDesktop.getConnectionFor` 调用均在既有路径——`isAttachedSharedRemote`
  对桥缺失有优雅回退（`!desktop?.getConnectionFor` → 返回 false 走原路由）；
  `requestGatewayForAgent` 等三处 "cannot dial registry connections" throw
  为既有行为（上个基准已存在），Web 端这些 agent-dial 路径不在
  WebCapabilityAdapter 能力范围（多连接 registry 桥未实现，行为与上版一致）。

## 补丁保留抽查

上游未触碰 §4 登记的 9 个带补丁文件（styles.css / chat/index.tsx /
global.d.ts / i18n×3 / gateway-settings.tsx / first-run-remote-form.tsx /
boot-failure-overlay.tsx / use-keybinds.ts / lib/media.ts /
use-composer-actions.ts），逐项 grep 确认补丁全部保留 ✓

## 其他核对

- vendor `package.json` 无变化 → 无需 `pnpm install`
- vendor `vite.config.ts` 有变化（manualChunks 加入
  `@tanstack/react-query` + dedupe）——上游为修复其打包双 react-query
  实例问题；apps/web/vite.config.ts 使用独立的 Web 构建配置，其
  manualChunks/dedupe 策略不镜像 vendor（Web 入口不产生该问题形态），
  无需对账
- 上游新增测试文件（`*.test.ts(x)`）全部命中 tsconfig `vendor/**/*.test.*`
  exclude；`e2e/`、`electron/` 新文件不受 tsconfig include（只含 vendor
  `src/`）影响
- `@hermes/shared` 子路径 import 集合无变化（shared 无实际变更）

## 验证计划

- 本机 `AGENTS.local.md` 禁止跑 typecheck/test/build；只运行格式化与轻量
  git/文本检查
- 真实验证走 GitHub Actions `CI`
