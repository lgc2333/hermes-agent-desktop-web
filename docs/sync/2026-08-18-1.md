# Sync 2026-08-18-1 — upstream main e624e9f

- 日期: 2026-08-18
- 上游: `NousResearch/hermes-agent` main `e624e9fde561`(08-18)
- 上一基准: `9ed4a7c`(2026-08-17 同步)
- 方式: 手工三路(见 PATCHES.md §7「git-subtree 失效根因」)

## 范围

- desktop 净变更 **136 文件, +13078/−1981**:
  translucency 窗口玻璃、kanban completion-notify、settings-scope、
  gateway-connection-lifecycle、cron-model-impact 等上游新增。
- **shared 也变了**: 新增 `src/translucency.ts` + `index.ts` 导出
  (base 树 `bdedfeb` → new 树 `4e6f373`)。

## 补丁处理

12 个 §4 补丁文件分两类:

| 处理                                                 | 文件                                                                                                      | 说明            |
| ---------------------------------------------------- | --------------------------------------------------------------------------------------------------------- | --------------- |
| 保留 HEAD 版(上游未变)                               | chat/index.tsx、first-run-remote-form、boot-failure-overlay、use-keybinds、use-composer-actions、media.ts | 6 个            |
| 3-way merge(base=9ed4a7c / ours=HEAD / main=e624e9f) | styles.css、global.d.ts、gateway-settings.tsx、i18n/en、zh、types                                         | 6 个,全部 clean |

Web 专有表面逐一核对保留: passwordLogin / oauthPaste / streamMediaUrl /
saveImageFile / releaseBlobFile、@source 两行、passwordSignIn / pasteSignIn。

## 修复记录(CI 暴露)

1. **shared 漏同步** → `Cannot find module '@hermes/shared/translucency'`
   - appearance-settings 连锁隐式 any(GLASS_MATERIALS/SCOPES 无类型)。
     修复: 同步 vendor/hermes-shared 两文件。
2. **tsconfig/vite 缺子路径别名** → 上游 shared/package.json 无
   `./translucency` 导出(桌面端靠 tsconfig paths),我们也没映射 → TS2307。
   修复: apps/web/tsconfig.json + vite.config.ts 加
   `@hermes/shared/translucency`(billing 同款)。
3. **3-way 文本无冲突 ≠ 语义无冲突** → 上游删除 gateway-settings 的
   `scope` 概念(useState 定义删了),我们的补丁引用
   `getConnectionConfig(scope)` 因区域不重叠被 merge-file 无冲突保留 →
   悬空引用 TS2304/TS7006。修复: 适配为 `getConnectionConfig(null)`、
   删 `saveConnectionConfig` 的 `profile: scope` 字段。
4. **桥面契约同步** → 上游 global.d.ts `setTranslucency` 签名
   `{intensity}` → `TranslucencyState`。修复: apps/web/src/bridge/denied.ts
   参数类型同步(布尔门,ADR-0010)。

## 提交链

- `8c965db` sync vendor/hermes-desktop to upstream main e624e9f(手工三路)
- `75360d5` sync hermes-shared + 适配 gateway-settings scope 删除
- `b8422b4` translucency 别名 + setTranslucency 类型
- `c3f7d45` PATCHES.md 同步备注

## 后续

上游 main 已到 `acc614e7`,但 desktop/shared 树与 e624e9f 相同(仅 vendor
外改动),无需再追。
