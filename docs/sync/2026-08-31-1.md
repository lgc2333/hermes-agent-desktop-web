# Sync 2026-08-31-1 — upstream main 44656326

- 日期: 2026-08-31
- 上游: `NousResearch/hermes-agent` main `446563262e54` (2026-08-31)
- 上一基准: `71c823bbf70e` (2026-08-29 同步)
- 方式: **git subtree merge** (`scripts/sync-upstream.sh main`)
  - desktop filtered commit: `b87dd8f382292818bc3b33808e8ab48c8bda7f64`（anchor 已挂 ref 保护）
  - shared filtered commit: `6fc43509c2e6c137a34706e3e4abe4c6db5043bc`（anchor 已挂 ref 保护）

## 变更

- desktop: 45 文件，+1788/−55
  - 新增 `electron/mcp-oauth-callback-ipc.ts`（MCP OAuth 客户端 loopback 回调
    监听，`hermesDesktop.mcpOauth` 桥面）+ `browser-real-profile-panel` 设置面板
  - bot-mode Group Chat 传输（同网关免 Desktop 运行、跨网关 scoped 传输）、
    delegation 失败子代理状态上报修正、compression split-failure cooldown、
    todos store/relay 预算、`todo.updated` gateway 事件（shared +1 行）等
- shared: 1 文件，+1（`json-rpc-gateway.ts` 新增 `todo.updated` 事件名）
- subtree 合并冲突: 无（两棵子树均干净 squash）

## Web 桥面处理

上游 `global.d.ts` 新增可选 `hermesDesktop.mcpOauth`
（`listen()/wait()/cancel()`，桌面 electron main 在**用户本机** loopback 起一次性
HTTP 监听接收 MCP provider 授权重定向，用于远程后端拓扑；渲染层消费点
`plugins/hermes-bots/mcp-setup.tsx`）:

- **不实现，类 3 拒绝面（已做）**: 浏览器不能 bind TCP 端口，web 拓扑下代理机器
  ≠ 用户浏览器所在机器，gateway REST 无等价端点（`client_redirect_uri` 网关侧
  只收 `127.0.0.1/localhost/::1` 的 http URL，`tui_gateway/mcp_oauth_sessions.py`
  `_validate_client_redirect_uri`）。`denied.ts` 加 `mcpOauth` 存根
  （listen/wait 恒 reject、cancel 返回 false），`adapter.ts` 接线，断言同步。
- **回退安全**: mcp-setup.tsx 对桥缺失/listen() 失败已有 legacy 回退——不发
  `client_redirect_uri`，走 gateway 侧 loopback 监听 + `mcp.servers.oauth.poll`
  轮询，与旧版 Web 行为一致，语义安全。

## 补丁保留抽查

上游触碰 4 个带补丁文件（styles.css / chat/index.tsx / global.d.ts / i18n
en+zh+types），逐一确认:

- `styles.css` Tailwind `@source` 两行保留 ✓
- `chat/index.tsx` voice `enabled: true` + Web 注释保留 ✓
- `global.d.ts` Web 专有表面（oauthPaste / passwordLogin / saveImageFile /
  releaseBlobFile / streamMediaUrl）保留 ✓
- i18n M5/M6 键（authUsername / authPassword / authPaste*，en/zh/types 各 4 处
  命中）保留 ✓
- `gateway-settings.tsx` / `first-run-remote-form.tsx` / `boot-failure-overlay.tsx`
  / `use-keybinds.ts` / `lib/media.ts` 上游未触碰 ✓（voice: chat/index.tsx 已核对）

## 其他核对

- vendor `package.json` / `vite.config.ts` 无变化 → 无需 `pnpm install`
- `browser-real-profile-panel.tsx`（新增面板）零 `hermesDesktop` 桥面引用，Web
  侧无需接线
- 上游新增 vendor 测试文件（含 `electron/` 下 2 个）全部命中既有 tsconfig
  exclude glob（`vendor/**/*.test.*`）；tsconfig include 只含 vendor `src/`，
  `electron/` 不受检，无配置改动
- `@hermes/shared` 子路径 import 集合无变化（shared 仅 +1 事件名常量），无新增
  别名需求

## 验证计划

- 本机 `AGENTS.local.md` 禁止跑 typecheck/test/build；只运行格式化与轻量
  git/文本检查
- 真实验证走 GitHub Actions `CI`
