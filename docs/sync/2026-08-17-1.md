# Sync 2026-08-17-1 — upstream main 9ed4a7c

- 日期: 2026-08-17
- 上游: `NousResearch/hermes-agent` main `9ed4a7c0251`(08-17)
- 上一基准: `cf64ca20`(引入基准)
- 方式: 手工三路(见 PATCHES.md §7「git-subtree 失效根因」)

## 范围

- desktop 净变更 **21 文件, +1865/−139**:
  hermes-bots 插件、budgeted-loop、image-generation-placeholder、
  connection-registry 等上游新增。
- shared 未变(base 树 == new 树)。

## 补丁处理

12 个 §4 补丁文件上游 base→new **全部未变**,直接保留为 HEAD 版
(无冲突、无 3-way)。

## 流程

新 desktop 过滤提交(`128cbb93`,树 = 上游 apps/desktop@9ed4a7c)
read-tree 挂到 vendor/hermes-desktop/,补丁文件 cacheinfo 恢复 HEAD 版。

## 清理

浅取副作用: 已 `git update-ref -d refs/tags/main`(若存在)、
`rm -f .git/shallow`、`git gc`(未 prune 过滤提交——避免再次破坏
subtree split,后续同步继续用手工三路)。
