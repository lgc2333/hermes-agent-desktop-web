# Sync 2026-08-29-1 — upstream main 71c823bb

- 日期: 2026-08-29
- 上游: `NousResearch/hermes-agent` main `71c823bbf70e` (08-29)
- 上一基准: `8c098e9e8191` (2026-08-28 同步)
- 方式: **git subtree merge** (`scripts/sync-upstream.sh main`)
  - desktop filtered commit: `199b7dfce144d60340a5e70f99cefd4a167c9114`
  - shared filtered commit: `efc5c20d74499a70e6c739275c6e8d458e9507f3`

## 变更

- desktop: 301 文件，+39773/−33120
  - 上游 HUD 拖拽重构（beginMove/endMove + solid 窗口标志）、gateway-file-download
    传输层重构、pane-mirror/preview-tile 清理、row-geometry、LRU cache、
    chat-empty-slot、transcript provenance、session-info gateway event 等
- shared: 无变化（树 SHA 相同，仅重挂 anchor）

- subtree 合并冲突: 无（两棵子树均干净 squash）

## Web 桥面处理

上游 `global.d.ts` 的 `hermesDesktop.hud.windowing` 新增 `solid` 标志、
`hud` 新增 `beginMove()/endMove()`、`moveBy` 签名改为 `{width,height}`（去 x/y）：

- **类型同步（已做）**: `apps/web/src/bridge/denied.ts` 补 `windowing.solid: false`、
  `beginMove()/endMove()` no-op，`moveBy` 参数形收窄；`adapter.test.ts` 断言同步。
- **不实现**: 三者均为 Electron OS 窗口能力（HUD 窗口拖动 / 窗口 solid 样式），
  浏览器无等价接口，gateway REST 也无对应端点 → 类 3 拒绝面（denied.ts no-op），
  与既有 hud 窗口管理成员同语义。

## 补丁保留抽查

本轮上游触碰 6 个带补丁文件（styles.css / chat/index.tsx / global.d.ts /
i18n en+zh+types），逐一确认：

- `styles.css` Tailwind `@source` 两行保留 ✓
- `chat/index.tsx` voice `enabled: true` + Web 注释保留 ✓
- `global.d.ts` Web 专有表面（oauthPaste / passwordLogin / saveImageFile /
  releaseBlobFile / streamMediaUrl）保留 ✓
- i18n M5/M6 键（authUsername / authPassword / authPaste*，en/zh/types 各 6 处
  命中）保留 ✓
- `gateway-settings.tsx` / `first-run-remote-form.tsx`（密码表单 + paste-back）、
  `boot-failure-overlay.tsx`（signInRemote 路由）、`use-keybinds.ts`
  （VITE_WEB_BUILD 条件展开）、`use-composer-actions.ts`（附件字节三处）、
  `lib/media.ts`（streamMediaUrl 委托）——本轮上游未触碰，无需操作 ✓

## 其他核对

- 上游新增 `plugins/hermes-bots/group-test-utils.ts`（Node `setImmediate`，web lib 无
  该类型）仅被 vendor 测试文件引用 → apps/web tsconfig exclude 追加
  `*test-utils.*` / `*test-helper.*` / `*test-util.*` 三条 glob（PATCHES.md §7 先例，
  不改 vendor）
- `@hermes/shared` 子路径 import 集合与上一基准相同（billing / billing-policy /
  charge-settlement / skin / translucency），shared 为 workspace 包 + exports
  map 未变，无新增别名需求
- vendor package.json 仅 engines 放宽 + 移除 plugins 测试 script，无依赖变化
- 上游新增 vendor 测试文件已按既有 tsconfig `exclude: vendor/**/*.test.*` 隔离

## 验证计划

- 本机 `AGENTS.local.md` 禁止跑 typecheck/test/build；只运行格式化与轻量 git/文本检查
- 真实验证走 GitHub Actions `CI`
