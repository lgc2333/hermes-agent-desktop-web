# Sync 2026-09-21-1 — upstream main 080907e3

- 日期: 2026-09-21
- 上游: `NousResearch/hermes-agent` main `080907e3b7ad` (2026-09-21)
- 上一基准: `498abb677ec3` (2026-09-15 同步)
- 方式: **git subtree merge** (`scripts/sync-upstream.sh main`)
  - desktop filtered commit: `23e069324399`（anchor 已挂 ref 保护）
  - shared filtered commit: `a30e618543bd`（anchor 已挂 ref 保护）
- 变更规模: 上游区间 1 commit。vendor desktop 310 文件变更；vendor shared 10 文件变更。

## 变更

- desktop:
  - 路由与配置扩展：支持配置档案默认路由（default profile route）、新窗口按路由打开；
  - 窗口与系统控制：添加 WSLg 自定义窗口控件（`windowControls`：minimize/toggleMaximize/close）、macOS 原生截图支持（`screenshot`）；
  - 插件管理：支持移除独立桌面插件（`removeDesktopPlugin`）；
  - 连接与网关管理：协程池销毁事件（`onPoolBackendRetiring`）、连接设置子页面重构与远程更新面板（`GatewayManagedUpdates`）；
  - 聊天输入与会话历史交互增强：草稿恢复提示、历史窗口回溯展开（`useHistoryWindow`）、浮动输入框等。
- shared:
  - OpenRPC / gateway contract 更新；
  - JSON-RPC 通道与网关连接/重试逻辑增强。

## Web 桥面处理（新接口）

1. `profile.getDefault()` / `setDefault(route)` / `onDefaultChanged(cb)`：实现，基于 localStorage 保存与同源事件广播。
2. `windowControls`：提供浏览器空实现（`custom: false`，minimize/toggleMaximize/close 为 no-op）。
3. `openWindow(options?: DesktopProfileRoute)`：对齐类型签名，在新标签页中打开。
4. `screenshot` / `removeDesktopPlugin` / `onPoolBackendRetiring`：均为桌面专属或可选 IPC 表面，Web 维持缺省/可选安全回退。

## 补丁保留抽查（§4）

全部打补丁文件逐项核对与合并：

- `src/styles.css`：Tailwind @source 引用保留。
- `src/app/chat/index.tsx`：合并正常，voice 配置与桥接保留。
- `src/app/chat/hooks/use-composer-actions.ts`：三方冲突已手动解决，保留 ADR-0020 `saveImageFile` 与回退机制。
- `src/global.d.ts`：Web 专有接口（密码登录、paste-back 等）完整保留。
- `src/app/settings/gateway-settings.tsx`：密码/paste-back 表单与 Web 修复逻辑保留。
- `src/components/first-run-remote-form.tsx`：密码/paste-back 逻辑保留。
- `src/components/boot-failure-overlay.tsx`：路由至嵌入式 Gateway settings 逻辑保留。
- `src/app/hooks/use-keybinds.ts`：`VITE_WEB_BUILD` 原生查找豁免逻辑保留。
- `src/i18n/en.ts` / `zh.ts` / `types.ts`：Web 翻译键保留。
