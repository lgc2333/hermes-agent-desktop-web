# Sync 2026-08-25-1 — upstream main 76e306c

- 日期: 2026-08-25
- 上游: `NousResearch/hermes-agent` main `76e306c45843` (08-25)
- 上一基准: `ec5e369fe68f` (2026-08-24-1 同步)
- 方式: **git subtree merge** (`scripts/sync-upstream.sh main`)
  - desktop filtered commit: `79186961826909abdb114d5b583d8efc9d8f755e`
  - shared filtered commit: `ad91bf29d5ae40e0951e4381f3a5978e08fd301f`

## 变更

- desktop: 107 文件，+6384/−588
  - 上游新增/调整 HUD windowing/native drag、backend claim/ready、zoom 保留、clarify hydration/replay、JSON-RPC recovery、vibe hearts、bot group turn lease 等
- shared: 2 文件，+633/−0
  - `json-rpc-gateway` 新增 replay/recovery 相关逻辑与测试
- subtree 合并零文本冲突；本次依赖文件无变更，未更新 lockfile

## Web 桥面处理

- 新上游 HUD 桥字段/方法：`hud.nativeDrag`、`hud.windowing`、`hud.setWorkspaceTransfer`、`hud.resetLayout`
  - Web 无 Electron HUD 原生窗口/拖拽/工作区转移语义，也无 gateway 等价端点
  - 已在 `apps/web/src/bridge/denied.ts` 按拒绝面实现合法空形状：字段恒 false，`setWorkspaceTransfer` no-op，`resetLayout` 返回 `{ ok: false }`
  - 已在 `apps/web/src/bridge/adapter.test.ts` 补订阅/形状断言
- 保留 Web 专有接口：`oauthPasteConnectionConfig`、`passwordLoginConnectionConfig`、`saveImageFile`、`releaseBlobFile`、`streamMediaUrl`

## 补丁保留抽查

- `global.d.ts` Web 专有 bridge 扩展保留，并合入上游 HUD windowing 扩展 ✓
- `styles.css` Tailwind `@source` 补丁保留 ✓
- `use-keybinds.ts` Web 构建不注册 `view.findInPage` 保留 ✓
- `lib/media.ts` `streamMediaUrl?.(path)` 优先保留 ✓

## 验证计划

- 本机 `AGENTS.local.md` 禁止跑 typecheck/test/build；只运行格式化与轻量 git/文本检查
- 真实验证走 GitHub Actions `CI`
