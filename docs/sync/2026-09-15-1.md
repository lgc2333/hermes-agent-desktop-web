# Sync 2026-09-15-1 — upstream main 498abb67

- 日期: 2026-09-15
- 上游: `NousResearch/hermes-agent` main `498abb677ec3` (2026-09-14)
- 上一基准: `d62716c7043e` (2026-09-13 同步)
- 方式: **git subtree merge** (`scripts/sync-upstream.sh main`)
  - desktop filtered commit: `fce10f76e8dd`（anchor 已挂 ref 保护）
  - shared filtered commit: `a42e0cd2704c`（anchor 已挂 ref 保护）
- 变更规模: 上游区间 20 commits。vendor desktop 264 文件变更（+6089/−3869）；vendor shared 28 文件变更（+41763/−351，主要包含 OpenRPC 生成文件 gateway-contract 等）。

## 变更

- desktop 渲染层重构：
  - blocking input family（clarify / approval / sudo / secret / vault / mcp setup）与 server read-back（terminal/preview/window/tour）从原有的 `desktop-bridge.ts` 与 `input-requests.ts` 重构分流至专门的 `server-requests.ts`；
  - 增加 `reasoning-pill` 及推理相关交互面板；
  - 增加 `transcript-video` 与 `video-playback-speed`；
  - 增加 IME 组合输入支持（`lib/ime.ts`）；
  - `model-options`、`model-menu-panel` 重构拆分 `use-model-menu-controller`；
  - 提取多项共享逻辑至 `@hermes/shared`（ansi、color、format、fuzzy、i18n、json-rpc-channel、model-search-text、reasoning-effort、reconnect-backoff、slash、theme-presets 等）。
- shared：
  - 包含 OpenRPC 规范及契约生成代码 `gateway-contract.generated.ts` 和 `gateway-contract.openrpc.json`；
  - 重新组织 package.json exports。

## Web 桥面处理（新接口）

- 本次上游未新增或删除 `window.hermesDesktop` 的必选方法，无需修改 Web 桥适配器。
- 原位改动（§4）经 3-way merge 均已保留且与上游 anchor 对齐一致。

## 补丁保留抽查（§4）

全部 12 个打补丁文件逐项验证无遗漏：

- `src/app/chat/hooks/use-composer-actions.ts`
- `src/app/chat/index.tsx`
- `src/app/hooks/use-keybinds.ts`
- `src/app/settings/gateway-settings.tsx`
- `src/components/boot-failure-overlay.tsx`
- `src/components/first-run-remote-form.tsx`
- `src/global.d.ts`
- `src/i18n/en.ts`
- `src/i18n/types.ts`
- `src/i18n/zh.ts`
- `src/lib/media.ts`
- `src/styles.css`
