# Sync 2026-09-22-1 — upstream main 6a4d5e33

- 日期: 2026-09-22
- 上游: `NousResearch/hermes-agent` main `6a4d5e33ce3e` (2026-09-22)
- 上一基准: `7b660e66ee3d` (2026-09-21-2 同步)
- 方式: **git subtree merge** (`scripts/sync-upstream.sh main`)
  - desktop filtered commit: `69813ac175c3`（anchor 已挂 ref 保护）
  - shared filtered commit: `0d05267615b9`（anchor 已挂 ref 保护）
- 变更规模: 上游区间 154 commit。vendor desktop 343 文件变更（+6528/-2079），新增 16 文件；vendor shared 2 文件（+13/-5）。
- 合并结果: 两个子树均自动合并（ort），零文本冲突。

## 变更

- electron（桌面主进程，Web 不加载）:
  - 新增 `notification-linux.ts`：Linux 通知改走 DBus（`org.freedesktop.Notifications`），取代原 native 通知桥（新依赖 `dbus-native@0.15.2`）；
  - 新增 `log-rotation.ts`：`desktop.log` 分档轮转 + 超限直接丢弃（曾观测到单文件 ~326GB 撑爆磁盘，连带 update/install 失败）；
  - 新增 `linux-crash-diagnostics.ts`：把 Chromium 日志重定向到文件、保留 minidump，让 Linux 崩溃带上自己的 FATAL 行；
  - 新增 `host-backend-singleton.ts`：一个 host 只跑一个 `hermes serve`（取代 per-profile backend pool，profile 走请求参数）。
- 渲染层:
  - 新增 composer 状态抽屉（`src/app/chat/composer/status-stack/drawer.tsx` + `hooks/use-status-drawer.ts` + `store/composer-status-drawer.ts`，i18n `statusStack.hideStack` / `showStack`）；
  - `src/api/config.ts`：配置写路由绑定读取来源（`bindConfigReadOrigin` / `retainConfigReadOrigin` / `resolveConfigWriteScope`）——写请求带上服务该次 GET 的 `(connectionId, profile)`，避免注册表主连接变更后写错机器；
  - `src/app/chat/history-window.ts`（229 行）与若干 composer/settings 文件重写；`src/plugins/hermes-bots/*` 群轮次调整；
  - 其余大量文件是上游 `npm run fix` 的格式化（如 `src/app/settings/index.tsx` / `subpages.ts`）。
- shared: `ProfileMirrored.auth` 类型由 `boolean` 放宽为 `boolean | 'shared'`（share_auth 下为 `"shared"`），`gateway-contract.generated.ts` / `openrpc.json` 同步再生成。

## Web 桥面处理（新接口）

本次 `.d.ts` 与 `hermesDesktop` 表面**零变更**，无新桥面需实现或对齐：

- 新依赖 `dbus-native` 仅被 `electron/notification-linux.ts`（Electron 主进程）引用，Web 构建不加载；锁文件已更新。
- `window.hermesDesktop.api` 调用新增 `connectionId` / `profile` / `priority` 作用域字段（配置读写路由绑定）——Web 桥 `webApi` 按既有语义处理：`getConnectionById` 未命中回落主连接（`registry.ts`），Web 单网关拓扑下行为不变，无桥层改动。
- Web 专有桥面（`oauthPasteConnectionConfig` / `passwordLoginConnectionConfig` / `saveImageFile` / `releaseBlobFile` / `streamMediaUrl`）上游未触及，原样保留。

## 补丁保留抽查（§4）

`git diff refs/subtree-anchors/hermes-desktop HEAD:vendor/hermes-desktop` 结果恰好是 §4 登记的 12 个补丁文件（+498/-74），无多无少；shared 无补丁、diff 为空。

- 与上游重叠的 3 个补丁文件（三方合并后共存，无重名/悬空引用）：
  - `src/app/chat/index.tsx`：上游仅格式重排，voice `enabled: true` 与注释保留；
  - `src/components/first-run-remote-form.tsx`：上游仅 `<pre>` 换行，密码表单与 paste-back 保留；
  - `src/i18n/{en,types,zh}.ts`：上游新增 `statusStack.hideStack` / `showStack`，与我们的 `authUsername` / `authPassword` / `authPaste*` 键同处一节、互不覆盖。
- web.css 覆盖锚点核对：`statusbar` / `composer-dock` / `composer-surface` / `composer-fade` / `aui_intro` / `auto-rows-fr grid-cols-1`（连接模式卡片）在新上游树中均存在，无静默失效。
- `apps/web/index.html` 与 vendor 版逐字节一致（上游本次未改 index.html）。
- 依赖：仅 vendor 侧新增 `dbus-native` → 已 `pnpm install` 更新锁文件（+27 行，apps/web 依赖树无变化）。

## 验证计划

- 本机 `AGENTS.local.md` 禁止跑 typecheck/test/build；只运行 `prettier -c .`（本地已过）。
- 真实验证走 GitHub Actions `CI`（unit / typecheck / lint / format / build / deno / e2e）。
