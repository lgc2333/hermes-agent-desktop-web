# Sync 2026-08-26-1 — upstream main c427367

- 日期: 2026-08-26
- 上游: `NousResearch/hermes-agent` main `c427367938ea` (08-26)
- 上一基准: `76e306c45843` (2026-08-25-1 同步)
- 方式: **git subtree merge** (`scripts/sync-upstream.sh main`)
  - desktop filtered commit: `0911f8efb62845e620b290f84eb280a6d1551591`
  - shared filtered commit: `192b87fc6afa137353f53fb5001ade866e865229`

## 变更

- desktop: 170 文件，+15064/−1091
  - 上游新增/调整 multi-connection/session ownership routing、Browser pop-out window、secret storage encryption toggle、profile remember、terminal/remote lifecycle 等
- shared: 1 文件，+1/−1
  - `json-rpc-gateway` error 状态设置时序调整
- subtree 合并冲突:
  - `vendor/hermes-desktop/src/global.d.ts`
  - `vendor/hermes-desktop/src/components/boot-failure-overlay.tsx`

## Web 桥面处理

- 新上游桥接口 `openBrowserWindow(tabId)` / `onBrowserPopoutClosed(cb)`
  - Web 已实现：同源新 tab 打开 `?win=browser&tab=<id>#/`；opener 侧监控 `Window.closed` 后用 `BroadcastChannel` best-effort 通知主 tab dock 回
- 新上游桥接口 `getSecretStorageEncryption()` / `setSecretStorageEncryption(on)`
  - Web 已实现安全降级：浏览器无 OS keychain，恒返回 `{ on: false }`
- 新上游桥接口 `profile.remember(name)`
  - Web 已实现：复用本地 profile preference 存储
- 上游 `oauthLogoutConnectionConfig(remoteUrl)` 改为必传 URL
  - Web 已透传 URL，并扩展 proxy OAuth/password logout endpoint：传 `{ target }` 时只清当前 gateway cookie；空 body 兼容旧“全清”行为
- 新上游可选 `setActiveConnectionRoute(route)`
  - Web 已实现 no-op；当前 SPA 没有 Electron 主进程窗口路由状态需要同步

## 补丁保留抽查

- `global.d.ts` Web 专有 `oauthPasteConnectionConfig` / `passwordLoginConnectionConfig` / `saveImageFile` / `releaseBlobFile` / `streamMediaUrl` 保留 ✓
- `boot-failure-overlay.tsx` 保留 Web 专有统一路由到嵌入式 Gateway settings；合入上游“仅清当前 gateway stale auth”语义 ✓
- `styles.css` Tailwind `@source` 补丁保留 ✓
- `gateway-settings.tsx` 密码 provider 表单、OAuth paste-back 与登录前保存 URL 语义保留 ✓
- `first-run-remote-form.tsx` 密码 provider 表单与 OAuth paste-back 保留 ✓
- `use-keybinds.ts` Web 构建不注册 `view.findInPage` 保留 ✓
- `use-composer-actions.ts` / `lib/media.ts` 附件与媒体 Web 桥优先逻辑保留 ✓

## 验证计划

- 本机 `AGENTS.local.md` 禁止跑 typecheck/test/build；只运行格式化与轻量 git/文本检查
- 真实验证走 GitHub Actions `CI`
