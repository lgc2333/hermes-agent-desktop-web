# Sync 2026-08-24-1 — upstream main ec5e369

- 日期: 2026-08-24
- 上游: `NousResearch/hermes-agent` main `ec5e369fe68f` (08-24)
- 上一基准: `933c209e9663` (2026-08-23-1 同步)
- 方式: `git subtree merge --squash`
  - 本次 Git/codeload 均被 GitHub 429 限制；改用 GitHub Git API + 上一纯上游 anchor 重建 `apps/desktop` 树，校验树 SHA `d5f71c69cf5897294364eace78052391660a3924` 后生成 filtered commit `9771f237dd076c055525c3ff8fca950a696385c6` 再 merge
  - `apps/shared` 上游树未变（`a842d02ebf8a4ef52ca71f851844ff9f7680114c`），未重建/merge shared

## 变更

- desktop: 197 文件，+17892/−1732
  - 上游新增/调整 registry-scoped REST/WS/下载路由、profile remote override、HUD game overlay、voice direct client、bot roster/group 逻辑等
  - 上游 `vendor/hermes-desktop/package.json` 仅变更 `@playwright/test` devDependency 版本；本仓库未跑 `pnpm install`，lockfile 待 CI 若失败再处理
- shared: 无内容变化
- subtree 合并零文本冲突

## Web 桥面处理

- 新增 `hud.onGameOverlay`：Web 无 Electron HUD/game-overlay 原生窗口面，按拒绝面实现为空订阅（`denied.hud.onGameOverlay`）
- `saveGatewayFile` 新增 `connectionId` 参数：已实现，按 Web registry 选目标 gateway；同时 `webApi` 也按 `HermesApiRequest.connectionId` 选 registry 连接，支撑上游新增 connection-scoped 能力 API
- 已加桥层单测覆盖：`getConnectionById`、`webApi({ connectionId })`、`saveGatewayFile({ connectionId })`、`hud.onGameOverlay` 订阅形状
- 保留 Web 专有接口：`oauthPasteConnectionConfig`、`passwordLoginConnectionConfig`、`saveImageFile`、`releaseBlobFile`、`streamMediaUrl`

## 补丁保留抽查

- `global.d.ts` Web 专有 bridge 扩展保留，并合入上游 `saveGatewayFile.connectionId` / `hud.onGameOverlay` ✓
- `use-keybinds.ts` Web 构建不注册 `view.findInPage` 保留 ✓
- `lib/media.ts` `streamMediaUrl?.(path)` 优先保留 ✓
- `styles.css` Tailwind `@source` 补丁保留 ✓

## 验证计划

- 本机 `AGENTS.local.md` 禁止跑 typecheck/test/build；只运行格式化与轻量 git/文本检查
- 真实验证走 GitHub Actions `CI`
