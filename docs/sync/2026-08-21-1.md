# Sync 2026-08-21-1 — upstream main 17d1095

- 日期: 2026-08-21
- 上游: `NousResearch/hermes-agent` main `17d1095443bb`(08-21)
- 上一基准: `13ce0c5c675e`(2026-08-19-1 同步)
- 方式: **git subtree merge**(`scripts/sync-upstream.sh main`)

## 变更

- desktop: 387 文件, +29012/−11176
  - 结构性拆分: `use-message-stream/gateway-event`、`lib/chat-messages`
  - 新能力/UI: preview-act、favicon、HUD frost/glass、intro splash、voice menu、connector/card、updates fanout、translucency tint/fade
  - 新增依赖: `@babel/core`、`@rolldown/plugin-babel`、`@types/babel__core`、`babel-plugin-react-compiler`
- shared: 3 文件, +410/−26
  - 新增 `data-url-read-max.ts`
  - 扩展 `translucency.ts`：`fade`、book、Windows/macOS material 解析
- subtree 合并零文本冲突；`pnpm install --lockfile-only` 更新 lockfile

## Web 桥面处理

- 新上游桥面 `hud.setFrost(showing)`：Web HUD 属拒绝面，无浏览器/gateway 等价 → 将 denied HUD 从 `setVibrancy` 同步为 `setFrost`，返回 `{ ok:false }`
- 新上游事实位 `glassSupported` / `translucencySupported`：Web 无 Electron 原生窗口材质 → bridge 显式 `false`，避免 vendor 在非 Electron shell 里按 UA fallback 误开启 glass/translucency UI
- 新上游 `resolveFavicon`：浏览器 fetch favicon 受 CORS/站点差异影响，且调用方已有缺省图标 fallback → 暂不实现
- 新上游 `connections.updateAll`：Web 部署更新由部署系统负责，gateway 无通用 RPC → 暂不实现
- `selectPaths.profile` 类型兼容即可；Web 文件选择仍只返回 `web-blob://` 虚拟路径
- 保留 Web 专有接口：`oauthPasteConnectionConfig`、`passwordLoginConnectionConfig`、`saveImageFile`、`releaseBlobFile`、`streamMediaUrl`

## 补丁保留抽查

- `styles.css` 顶部 `@source '../../hermes-desktop/src'` / `@source '../../hermes-shared/src'` ✓
- `chat/index.tsx` voice `enabled: true` ✓
- `global.d.ts` Web 专有 bridge 扩展保留 ✓
- i18n `authUsername` / `authPassword` / `authPaste*`(en/zh/types) ✓
- `use-composer-actions.ts` `saveImageFile` / `attachFileBlob` / `releaseBlobFile` ✓
- `lib/media.ts` `streamMediaUrl?.(path)` 优先 ✓

## 验证计划

- 本机 `AGENTS.local.md` 禁止跑 typecheck/test/build；只运行了轻量/必要命令：
  - `bash scripts/sync-upstream.sh main` ✓
  - `pnpm install --lockfile-only` ✓
  - `pnpm format`（提交前格式化）
- 真实验证走 GitHub Actions `CI`。
