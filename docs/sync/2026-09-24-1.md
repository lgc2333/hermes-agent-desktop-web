# Sync 2026-09-24-1 — upstream v2026.9.24 f97608f

- 日期: 2026-09-24
- 上游: `NousResearch/hermes-agent` tag `v2026.9.24`（= v0.21.5）`f97608f178d1ffeca59860195ab7da295f7c8e5f`(09-24)
- 上一基准: `6a4d5e33ce3ed31eea9c25eb411da302ff752a9c`(2026-09-22-1 同步)
- 方式: **git subtree merge**(`scripts/sync-upstream.sh v2026.9.24`)
  - desktop filtered commit: `0acd97a9347d`(anchor 已挂 ref 保护)
  - shared filtered commit: `3fd5ce3c5d08`(anchor 已挂 ref 保护)
- 变更规模: 上游区间 1637 commit(compare `6a4d5e33...f97608f`)。vendor desktop 1151 文件变更(+73258/-27844;新增 240 / 删除 82);vendor shared 9 文件(+5827/-1352)。
- 合并结果: 两个子树均自动合并(ort),零文本冲突。

## 变更

- desktop(上游 ~460 PR 的 rollup,是本项目至今规模最大的一次同步):
  - 新 `src/app/capabilities/` 能力页重构(51 新文件):`connectors/`(MCP server / 插件 / 工具的目录页、详情 dialog、工具列表与编辑器、状态与探针)、`mcp/`(安装目录项、编辑器、日志、探针)、`plugins/`(插件包与设置表单)、`skills/`、`toolsets/`,取代原 `app/capabilities/mcp/mcp-tab.tsx`;
  - hermes-bots 新增 **Bot Screen**(18 新文件):`screen-pane.tsx` / `screen-connection.ts` / `screen-state.ts` / `screen-portal.tsx` / `screen-install.tsx` / `screen-autoraise.ts` —— 经 noVNC 观看 bot 的无头 Xfce 屏幕;新依赖 `@novnc/novnc@1.7.0`;
  - `src/store/interface-mode.ts`:Simple / Advanced 界面模式(显示层 resolver,只 shadow 不写用户偏好);
  - 新设置行:`minimize-to-tray-setting.tsx`(最小化到托盘)、`hud-modifier-settings.tsx`(HUD 修饰键)、`appearance-contrib.tsx`、`model-select.tsx`;
  - i18n 新增 `connectorsPage` / `interfaceMode` 两节;
  - 删除:旧 `app/capabilities/mcp/mcp-tab.tsx`、`components/pet/floating-pet-poll`、`app/overlays/peek-scope` 等模块,以及大量上游自带测试与 e2e spec(合并/重写);
  - `package.json`:+`@novnc/novnc@1.7.0`、electron-builder `afterPack` 钩子;删 `test:task-scroll` / `repro:short-session-hang:test` 脚本与对应 fixtures。
- shared: 9 文件 —— `json-rpc-gateway.ts`(+219/-…,重连与 replay)、新增 `json-rpc-gateway-replay.test.ts`、`gateway-events.ts`、`gateway-contract.generated.ts` 与 `gateway-contract.openrpc.json` 再生成(`display.*` 等契约)、若干测试删除。
- 零文本冲突;`pnpm-lock.yaml` 因新依赖更新(`pnpm install`)。

## Web 桥面处理

本次上游 `global.d.ts` 只新增 4 个**可选**桥面,全部是 Electron 主进程专属、无浏览器等价、gateway 无对应 REST 端点 → 一律不实现(Web 桥缺省,渲染层 optional-chain 兜底,设置行经 `available()` 隐藏):

- `hudModifier?: HudModifierApi` —— Electron HUD 覆盖层修饰键(`electron/hud-modifier-types.ts`);
- `minimizeToTray?`(get / set / onChanged)—— 系统托盘;
- `setF12ShortcutActive?` / `onF12Shortcut?` —— 主进程注册的全局 F12 热键(浏览器里 F12 归 devtools 自身)。

无新增**必填**桥面;`apps/web/src/bridge/` 本次零改动。新调用点(`readFileDataUrl` / `saveGatewayFile` / `openExternal` / `api`)均已有 Web 实现。

Bot Screen 的网关侧不需要 Web 改动:

- 它走 `display.*` JSON-RPC(既有 `hermesDesktop.api` 面)+ 兄弟 WebSocket `/api/display/ws`(noVNC 的 Websock 独占 socket,与流式 TTS 的 `/api/audio/speak-stream` 同理);
- 代理 WS 中继已透传客户端 pathname(ADR-0022,relay.ts `buildUpstreamWsUrl`),REST 转发按 **target** 白名单而非路径白名单(main.ts 分支 6)→ `/api/display/ws` 等新端点无需新增白名单项;
- `resolveSiblingWsUrl` 会取 `getConnectionFor` / `getGatewayWsUrlFor`(Web 未实现,见 2026-09-01-1):Web 单网关下 bot 行不带 route 描述符(`getProfileRoutes` 恒空表 → `not_scoped`)→ 回落 `desktop.getGatewayWsUrl()`,拿到代理 `/api/ws?target=…` 后换 pathname、剔除 `token` 即可。
- **已知边界(本次未改)**:若某 bot 行真带上 registry route 描述符(多连接 roster),`wsDeps` 退化为 `{}`,OAuth 模式会抛 "cannot refresh OAuth WebSocket tickets"。Web 拓扑下不出现;若将来需要,实现 `getConnectionFor` / `getGatewayWsUrlFor`(注册表按 id 查 + `wsUrlFor`)即可闭合。

保留 Web 专有接口:`oauthPasteConnectionConfig`、`passwordLoginConnectionConfig`、`saveImageFile`、`releaseBlobFile`、`streamMediaUrl`、`readFileDataUrl`(web-blob:// 分支)。

## 补丁保留抽查(§4)

`git diff --stat refs/subtree-anchors/hermes-desktop HEAD:vendor/hermes-desktop` 恰好是 §4 登记的 12 个补丁文件(+498/-74),无多无少;shared 无补丁、diff 为空。

- 与上游重叠、三方合并后共存的文件:`src/global.d.ts`(Web 专有面全保留 + 上游 4 个新可选面)、`src/app/chat/index.tsx`(voice `enabled: true` 及注释)、`src/styles.css`(Tailwind `@source` 两行)、`src/app/hooks/use-keybinds.ts`(Web 构建不注册 `view.findInPage`)、`src/lib/media.ts`(`streamMediaUrl?` 优先)、`src/app/settings/gateway-settings.tsx`、`src/components/first-run-remote-form.tsx`、`src/components/boot-failure-overlay.tsx`、`src/app/chat/hooks/use-composer-actions.ts`、`src/i18n/en.ts` / `zh.ts` / `types.ts`。
- `apps/web/index.html` 与上游 `vendor/hermes-desktop/index.html` 逐字节一致(上游本次未改该文件);`apps/web/vite.config.ts` 的别名与上游 vendored vite.config.ts 本次无需对账。

## 验证计划

- 本机 `AGENTS.local.md` 禁止跑 typecheck/test/build;只运行轻量/必要命令:
  - `bash scripts/sync-upstream.sh v2026.9.24` ✓
  - `pnpm install --lockfile-only`(新依赖 `@novnc/novnc` → 更新 lockfile;秒级完成,重跑无 diff 即一致)✓
  - `pnpm format` / `prettier -c PATCHES.md docs/sync/2026-09-24-1.md`(提交前格式化)✓
- 真实验证走 GitHub Actions `CI`。

## 坑:上游 phantom dependency —— `lucide-react`

- 现象:CI 红(typecheck / build / e2e 三处同因),`Cannot find module 'lucide-react'` /
  `Rolldown failed to resolve import "lucide-react"`,指向新文件
  `vendor/hermes-desktop/src/components/onboarding-chat/cards/setup.tsx`。
- 根因:上游 `apps/desktop` 源码直接 import 未在其 package.json 声明的包 —— 上游
  monorepo 根 `npm install` 把 `web/package.json` 的 `lucide-react@0.577.0` 扁平提升到
  根 node_modules,`apps/desktop` 白嫖到(phantom dep)。本仓库只 vendoring
  apps/desktop|shared,该包不存在。
- 处置:在 `apps/web/package.json` devDependencies 显式补 `lucide-react@0.577.0`
  (版本对齐上游 `web/package.json`),`pnpm install --lockfile-only` 更新 lockfile;
  **不改 vendor 的 package.json**(登记见 PATCHES §5)。

## 坑:全量 `pnpm install` 在本机挂起

- 现象:全量 `pnpm install` 跑到 ~13 分钟(lockfile 已写、`@novnc` 已落盘)后完全静止——事件循环 `epoll_wait` 空转、utime/io 计数 60s 零增长、无 socket、无子进程、stdin 为 `/dev/null`(非交互提示)。kill 后 `pnpm install --lockfile-only` 803ms 通过,lockfile 无 diff。
- 根因:未定位(非交互提问、非网络等待);link/build 阶段卡死,与本机资源无关的 pnpm 11.22 行为。
- 处置/教训:本机同步只需 lockfile(CI 自行全量安装),一律用 `pnpm install --lockfile-only`,不跑全量 install。
