# Sync 2026-08-23-1 — upstream main 933c209

- 日期: 2026-08-23
- 上游: `NousResearch/hermes-agent` main `933c209e96630`(08-23)
- 上一基准: `a4f16e3fefdc`(2026-08-22-1 同步)
- 方式: **git subtree merge**(`scripts/sync-upstream.sh main`)

## 变更

- desktop: 84 文件, +5850/−771
  - `fix(compression): -900k Codex variants...`
  - 新增/调整跨连接 profile route、remote bot routing、session request routing 与多处测试
  - runtime plugin loader 新增 `readPluginSource` 优先读取完整插件源码
  - roster agent 新增 `targetProfile`，用于区分本地显示 profile 与后端目标 profile
- shared: 上游树未变；脚本刷新了 filtered split/anchor，无实际内容差异
- subtree 合并零文本冲突；本次依赖无新增/删除，未更新 lockfile

## Web 桥面处理

- 新上游桥面 `readPluginSource?: (filePath) => Promise<HermesReadFileTextResult>`：Web 无本地 Electron plugin 文件路径，也无安全等价 browser/gateway 端点 → 不实现；runtime loader 已用 optional 能力并会退回 `readFileText`/truncated 保护
- 新字段 `DesktopRosterAgent.targetProfile?: string` / `DesktopPluginProfileRoute.targetProfile`：数据路由字段，不是新桥方法；Web `getProfileRoutes` 仍为空表，不需新增 adapter 逻辑
- 保留 Web 专有接口：`oauthPasteConnectionConfig`、`passwordLoginConnectionConfig`、`saveImageFile`、`releaseBlobFile`、`streamMediaUrl`

## 补丁保留抽查

- `global.d.ts` Web 专有 bridge 扩展保留，且已合入上游 `readPluginSource` / `targetProfile` ✓
- `gateway-settings.tsx` / `first-run-remote-form.tsx` 密码登录与 OAuth paste-back 保留 ✓
- `use-composer-actions.ts` `saveImageFile` / `attachFileBlob` / `releaseBlobFile` 保留 ✓
- `lib/media.ts` `streamMediaUrl?.(path)` 优先保留 ✓
- `use-keybinds.ts` Web 构建不注册 `view.findInPage` 保留 ✓
- Web `adapter.ts` `getProfileRoutes: async () => []` 与 Web 专有 bridge 实现保留 ✓

## 验证计划

- 本机 `AGENTS.local.md` 禁止跑 typecheck/test/build；只运行轻量/必要命令：
  - `bash scripts/sync-upstream.sh main` ✓
  - `pnpm format`（提交前格式化）
- 真实验证走 GitHub Actions `CI`。
