# Sync 2026-08-27-1 — upstream main 74ec63f

- 日期: 2026-08-27
- 上游: `NousResearch/hermes-agent` main `74ec63fe18fe` (08-27)
- 上一基准: `c427367938ea` (2026-08-26-1 同步)
- 方式: **git subtree merge** (`scripts/sync-upstream.sh main`)
  - desktop filtered commit: `7589b80baec32ec03b5bd578016b3e3c10b3e243`
  - shared filtered commit: `69f50840b697b1b0c6b9f1f31077aa12efd72cf8`

## 变更

- desktop: 161 文件，+16894/−834
  - 上游新增/调整 managed SSH update engine、fleet roster、read-only transcript、
    transcript tail cache 重构、connections registry lifecycle push 等
- shared: 无变化（树 SHA 相同，仅重挂 anchor）

- subtree 合并冲突: 无（两棵子树均干净 squash）

## Web 桥面处理

上游 `global.d.ts` 的 `hermesDesktop.connections` 新增三个**可选**成员：

- `onChanged(cb)` — **Web 已实现**（浏览器等价）：注册表本就在浏览器 localStorage
  （ADR-0002），GatewayAdapter 内置监听器集合，save/remove 写操作后自广播。
  reason 语义对齐桌面 main：新建或纯 label 改名 → `saved`（refresh push，#95393）；
  kind/url/authMode/token 实质编辑 → `updated`（次级 gateway dispose + redial）；
  删除 → `removed`（dispose + 清 owner hints）。消费方：use-gateway-boot.ts、
  connection-switcher.tsx、use-fleet-roster.ts。桥单测
  `gateway/index.test.ts` 覆盖四种触发路径与退订。
- `updateManaged(id)` — **不实现**（类 3 拒绝面语义，可选成员缺省）：桌面主进程
  专有事务通道（drain → 更新 → restore 受管 SSH 安装），浏览器无等价接口，
  gateway REST 也无对应端点。上游调用侧 `managedUpdater()` 有存在性守卫：
  缺省时 Settings 隐藏整个 managed updates 区块（fail-closed）。
- `updateAll(opts)` — **不实现**（同上）：向所有已注册连接扇出执行远端
  `hermes update` 命令，需桌面更新内核；上游 `updates.ts` 已有
  `bridge?.updateAll` 存在性守卫，缺省时跳过扇出分支。

## 补丁保留抽查

- `styles.css` Tailwind `@source` 补丁保留 ✓
- `global.d.ts` Web 专有表面（oauthPaste / passwordLogin / saveImageFile /
  releaseBlobFile / streamMediaUrl）保留 ✓
- `gateway-settings.tsx` / `first-run-remote-form.tsx` 密码表单 + OAuth paste-back
  保留 ✓（本轮上游未触碰这些文件）
- `boot-failure-overlay.tsx`、`use-keybinds.ts`、`use-composer-actions.ts`、
  `lib/media.ts`、i18n M5/M6 键（en/zh/types 各 8 处命中）保留 ✓

## 其他核对

- `@hermes/shared` 子路径 import 与上一基准相同（billing / billing-policy /
  charge-settlement / skin / translucency），别名均可解析，无新增映射需求

## 验证计划

- 本机 `AGENTS.local.md` 禁止跑 typecheck/test/build；只运行格式化与轻量 git/文本检查
- 真实验证走 GitHub Actions `CI`
